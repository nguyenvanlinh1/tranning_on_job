import { ConvertToSlugPipe } from './convert-to-slug.pipe';

describe('ConvertToSlugPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToSlugPipe();
    expect(pipe).toBeTruthy();
  });
});
